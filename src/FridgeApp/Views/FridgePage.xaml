<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
						 xmlns:local="clr-namespace:FridgeApp.ViewModels"
             x:Class="FridgeApp.Views.FridgePage">
	
	<ContentPage.Content>
		<StackLayout Spacing="3" Padding="15">
			<Label Text="Name" FontSize="Medium" />
			<Entry Text="{Binding Name, Mode=TwoWay}" FontSize="Medium" />

			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition></ColumnDefinition>
					<ColumnDefinition Width="20"></ColumnDefinition>
				</Grid.ColumnDefinitions>
				<Label Text="Partitions"></Label>
				<Button Grid.Column="1" Text="+"></Button>
			</Grid>

			<CollectionView x:Name="PartitionsView"
                ItemsSource="{Binding Partitions}"
                SelectionMode="None">
				<CollectionView.ItemTemplate>
					<DataTemplate>
						<Grid Padding="10" x:DataType="local:PartitionViewModel">
							<Grid.ColumnDefinitions>
								<ColumnDefinition></ColumnDefinition>
								<ColumnDefinition Width="20"></ColumnDefinition>
							</Grid.ColumnDefinitions>
							<Entry Grid.Column="0" Text="{Binding Name, Mode=TwoWay}" 
                            Style="{DynamicResource ListItemTextStyle}" 
                            FontSize="16" />
							<Button Grid.Column="1" Text="D"></Button>
						</Grid>
						
						<!--<StackLayout Padding="10" x:DataType="local:PartitionViewModel">
							<Entry Text="{Binding Name, Mode=TwoWay}" 
                            Style="{DynamicResource ListItemTextStyle}" 
                            FontSize="16" />
						</StackLayout>-->
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>


			<StackLayout Orientation="Horizontal">
				<Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
				<Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
			</StackLayout>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>